--- a/vllm/config/speculative.py
+++ b/vllm/config/speculative.py
@@ -655,6 +655,9 @@ class SpeculativeConfig:
         method instead.
         """
         if not callable(target_hf_overrides):
-            return SpeculativeConfig.hf_config_override
+            # Online draft quantization reads quantization metadata through
+            # ModelConfig.hf_overrides and requires a dict. The proxy draft
+            # configs are already normalized, so preserve them unchanged.
+            return {}
         return functools.partial(
             SpeculativeConfig._apply_composed_hf_override, target_hf_overrides
